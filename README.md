# waylogo
**waylogo** is a _port_ of xlogo, but instead of showing the X Org logo it shows the wayland logo.

## Build Instructions
This is a meson project so you can just run
```
meson setup build/
ninja -C build/
```
to build and
```
meson install -C build/
```
to install.
